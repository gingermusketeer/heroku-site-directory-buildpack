{
  "name": "heroku-site-directory-buildpack",
  "version": "1.0.0",
  "description": "Buildpack for serving a directory online as a normal folder structure.",
  "repository": "https://github.com/gingermusketeer/heroku-site-directory-buildpack",
  "license": "MIT",
  "dependencies": {
    "serve": "^11.3.0"
  },
  "scripts": {
    "start": "./node_modules/.bin/serve $DIRECTORY"
  }
}
